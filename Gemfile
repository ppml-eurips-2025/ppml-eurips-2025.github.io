# Gemfile
source "https://rubygems.org"

# Lock to GitHub Pages' plugin set (includes jekyll-remote-theme, etc.)
gem "github-pages", group: :jekyll_plugins

# Recommended by Minimal Mistakes for performance
gem "jekyll-include-cache"
